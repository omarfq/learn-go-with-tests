package integers

// When you have more than one argument of the
// same type, we can shorten it like the arguments below

// Add takes two integers and returns the sum of them.
func Add(x, y int) int {
	return x + y
}
